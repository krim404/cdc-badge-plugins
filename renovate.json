{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "packageRules": [
    {
      "description": "getrandom must track the major vodozemac resolves (0.2 via rand_core 0.6); the wasm custom backend is registered per major and a mismatch leaves vodozemac without entropy.",
      "matchManagers": ["cargo"],
      "matchPackageNames": ["getrandom"],
      "matchUpdateTypes": ["major"],
      "enabled": false
    }
  ]
}
